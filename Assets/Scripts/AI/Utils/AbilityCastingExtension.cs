using OrderElimination.AbilitySystem;

namespace AI.Utils
{
    public static class AbilityCastingExtension
    {
        public static void Cast(this ActiveAbilityRunner abilityRunner, IBattleContext battleContext, AbilitySystemActor caster)
        {
            // abilityRunner.InitiateCast(battleContext, caster);
            // abilityRunner.ConfirmTargeting();
        }
    }
}